FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 复制 package 文件
COPY package*.json ./

# 安装所有依赖（包括 devDependencies）
RUN npm install

# 暴露端口
EXPOSE 3000

# 默认命令（会被 docker compose 覆盖）
CMD ["npm", "run", "dev"]

